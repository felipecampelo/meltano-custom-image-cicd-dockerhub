#File to place global custom plugins configurations

# EXAMPLE: configuration of a custom tap named 'example'
plugins:
  extractors:
  - name: tap-rickandmorty
    namespace: tap_rickandmorty
    pip_url: ./plugins/custom/tap-rickandmorty
    capabilities:
    settings:
    config: